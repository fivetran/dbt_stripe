config-version: 2

name: 'stripe'
version: '0.0.1'

require-dbt-version: ">=0.17.0"

models:
    stripe:
        materialized: table
        intermediate:
            materialized: ephemeral
    stripe_source:
        database: 'culdesac-labs'

profile: integration_test
